version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo Installing dependencies
      - npm install

  build:
    commands:
      - echo Building the Flutter app
      - flutter build bundle

  post_build:
    commands:
      - echo Build completed

artifacts:
  files:
    - build/bundle/*
